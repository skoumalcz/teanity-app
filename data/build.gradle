plugins {
    id("com.android.library")
    id("kotlin-android")
}

dependencies {
    api(Lib.teanity("di", "one.zero-SNAPSHOT"))

    api(project(":model"))
    api(project(":network"))
    api(project(":persistence"))

    implementation(Lib.kotpref)
}