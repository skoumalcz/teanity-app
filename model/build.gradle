plugins {
    id("com.android.library")
    id("teanity")
}

teanity {
    modules {
        version = Lib.v.teanity

        usePersistence = true
        useNetwork = true
    }
}

dependencies {
    api(Lib.kotlin.lib)
}